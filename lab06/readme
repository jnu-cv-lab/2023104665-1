姓名：谢厚麟
学号：2023104665
实验主题：基于 OpenCV 的局部特征检测、描述与图像匹配
三、实验步骤与代码实现
1. 实验准备
将 box.png（模板图像）和 box_in_scene.png（场景图像）放入 lab06 文件夹，与代码文件同目录。
2. 完整实验代码：见实验附件
四、实验结果与分析
1. 特征点检测结果
图像	关键点数量	描述子维度
box.png	约 980 个	32 维（256bit）
box_in_scene.png	约 950 个	32 维（256bit）
特征点主要分布在图像的角点、纹理丰富区域，平坦区域几乎无特征点，符合 ORB 的检测逻辑。		
2. 特征匹配与 RANSAC 提纯结果
初始匹配数：约 600 对
RANSAC 内点数：约 380 对
内点比例：约 63%
RANSAC 有效剔除了约 37% 的误匹配，匹配对的几何一致性显著提升。
3. 目标定位结果
通过单应矩阵投影，模板图像的四个角点成功映射到场景图像中，绘制的绿色框准确包围了目标物体，定位效果良好。
五、实验问题
    问题 1：
    特征点主要分布在图像的哪些区域？为什么平坦区域几乎没有特征点？
特征点主要分布在图像的角点、边缘、纹理丰富区域，这些区域的灰度梯度变化显著。
平坦区域的灰度值变化极小，梯度几乎为 0，无法通过 FAST 算法检测到稳定的角点，因此几乎没有特征点。
    问题 2：
    什么是特征描述子？关键点和描述子的关系是什么？ORB 描述子的维度是多少？
特征描述子是用于描述特征点局部邻域信息的向量，可区分不同的特征点。
关键点包含特征点的位置、方向、尺度信息；描述子是关键点的 “特征指纹”，二者一一对应，关键点用于定位，描述子用于匹配。
ORB 的描述子维度为32 维（256bit 二进制串）。
    问题 3：
    ORB 特征匹配为什么使用 Hamming 距离？
ORB 的描述子是二进制串（0/1 组成），Hamming 距离衡量两个二进制串对应位不同的个数，计算效率极高，仅需按位异或和计数即可。
欧氏距离适用于浮点型向量，若用于二进制描述子，不仅计算效率低，也无法体现二进制串的差异特性，因此 ORB 匹配默认使用 Hamming 距离。
    问题 4：
    ORB 算法对旋转、平移、尺度变换是否具有鲁棒性？为什么？
对平移具有鲁棒性：描述子基于特征点的局部邻域计算，整体平移不影响局部邻域的信息。
对旋转具有鲁棒性：ORB 通过灰度质心法计算特征点的主方向，将描述子在主方向上进行旋转，保证描述子的旋转不变性。
对尺度变换具有一定鲁棒性：ORB 通过图像金字塔在不同尺度下检测特征点，可适应一定范围内的尺度变化。
对大视角透视变换不具备鲁棒性：透视变换会改变局部邻域的灰度分布，导致描述子相似度下降。
    问题 5：
    初始匹配结果中为什么会存在误匹配？仅靠描述子距离判断匹配是否可靠？
误匹配的主要原因：图像中存在相似纹理、重复图案或背景干扰，导致不同特征点的描述子相似度较高，被错误判定为匹配对。
仅靠描述子距离判断匹配不可靠：描述子相似度仅反映局部信息的相似性，无法保证匹配点在全局几何上的一致性，必须结合几何约束（如 RANSAC）剔除误匹配。
    问题 6：
    RANSAC 算法的作用是什么？Inlier 和 Outlier 分别代表什么？为什么 RANSAC 提纯后的匹配结果更合理？
RANSAC 的作用：通过随机采样和模型拟合，剔除不符合全局几何模型的误匹配，提升匹配的鲁棒性。
Inlier（内点）：符合单应矩阵模型的正确匹配点对；Outlier（外点）：不符合模型的误匹配点对。
提纯后的匹配结果同时满足 “局部描述子相似性” 和 “全局几何一致性”，有效消除了相似纹理等干扰带来的误匹配，因此更合理。
    问题 7：
    单应矩阵（Homography）的物理意义是什么？本实验中为什么可以用单应矩阵进行目标定位？
物理意义：描述平面物体在两幅图像间的投影变换关系，可实现图像间的点坐标映射。
本实验中，模板图像中的物体为平面物体，其在场景图像中的投影变换可通过单应矩阵准确描述，因此可通过求解单应矩阵，将模板图像的角点投影到场景图像中，实现目标定位。
局限性：单应矩阵仅适用于平面物体，若物体为非平面，投影变换为非线性，会导致定位误差增大。
    问题8：
    描述子类型不同：ORB 是二进制描述子，由 0/1 组成；SIFT 是浮点型描述子，为 128 维浮点数向量。
匹配距离不同：ORB 使用汉明距离（Hamming），只统计二进制位不同的个数；SIFT 使用欧氏距离（L2），计算浮点向量间的空间距离。
计算速度不同：ORB 运算更快，适合实时系统；SIFT 计算量大，速度更慢。
鲁棒性不同：SIFT 对旋转、尺度、光照、模糊的鲁棒性更强，匹配更稳定；ORB 鲁棒性稍弱，但足够满足一般目标定位与匹配需求。
专利与使用场景不同：ORB 开源免费，常用于工业、移动端实时检测；SIFT 曾受专利保护，更适合高精度视觉任务。
    问题 9：
    SIFT 算法是否对透视变换具有鲁棒性？为什么？
SIFT 算法对小视角的透视变换具有一定鲁棒性，但对大视角透视变换不具备严格鲁棒性。
原因：SIFT 通过尺度空间和主方向实现了尺度和旋转不变性，但透视变换会改变局部邻域的灰度分布和空间关系，当视角变化较大时，局部特征的相似度会显著下降，导致匹配失败。
本实验中，目标定位的鲁棒性主要依赖 RANSAC + 单应矩阵的几何约束，而非 SIFT 算法本身。
    问题 10：
   实验总结与收获：
本实验基于 OpenCV 完成了基于 ORB 特征的图像匹配与目标定位任务，实现了模板图像与场景图像的特征检测、匹配、误匹配剔除与目标框绘制，同时对比了不同特征数量及 SIFT 算法的效果，验证了局部特征匹配的完整流程。
ORB 特征检测与匹配的基本流程为：首先通过 FAST 角点检测并计算主方向，得到具备旋转不变性的特征点；再生成 256 位二进制描述子；随后通过暴力匹配结合汉明距离计算描述子相似度，完成初始匹配；最后结合几何约束提纯匹配结果。
RANSAC 与 Homography 是目标定位的关键：RANSAC 通过随机采样与模型拟合，剔除不符合全局几何关系的误匹配，仅保留内点；Homography（单应矩阵）则通过内点求解平面投影变换关系，将模板图像的角点映射到场景图像中，实现目标的精确定位。
实验中，我曾遇到图像读取失败、空矩阵报错的问题，通过修正文件路径并检查文件完整性得以解决；同时发现特征点数量并非越多越好，过多特征会引入噪声，降低匹配稳定性。
本次实验让我深刻理解了特征匹配鲁棒性的核心：鲁棒性不仅依赖描述子的局部相似性，更需要 RANSAC 等几何约束来保证全局一致性，二者结合才能在存在视角、光照变化的场景中实现可靠的目标定位。

